const host = 'http://localhost/'
//const path = ''

const paths = {
  httpServerUrl: host
}

const http_req = {
    GET: "GET",
    POST: "POST",
    PUT: "PUT",
    DELETE: "DELETE"
}

const _costants = { paths, http_req }

export default _costants;